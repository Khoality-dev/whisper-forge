services:
  whisperforge:
    build: .
    ports:
      - "7860:7860"
      - "6006:6006"
    volumes:
      - ./userdata:/app/userdata
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
